# Values do chart 2.63.1 / driver 1.63.1, não um objeto Kubernetes.
controller:
  region: sa-east-1
  replicaCount: 1
  nodeSelector:
    curso.ebs/controller: "true"
node:
  enableWindows: false
  nodeSelector:
    curso.ebs/node: "true"
