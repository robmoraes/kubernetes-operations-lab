apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: curso-sre
resources:
  - namespace.yaml
  - workloads.yaml
configMapGenerator:
  - name: prometheus-config
    files:
      - prometheus.yml
      - rules.yml
  - name: blackbox-config
    files:
      - blackbox.yml
