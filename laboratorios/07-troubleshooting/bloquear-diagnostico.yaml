apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: bloquear-diagnostico
  namespace: curso-incidentes
spec:
  podSelector:
    matchLabels: {papel: diagnostico}
  policyTypes: [Egress]
